{
    "id": "applicationManager.listAllHandlersForUrl",
    "type": "object",
    "properties": {
        "url": {
            "type": "string",
            "description": "The url for getting handler application by verb."
        }
    }
}
